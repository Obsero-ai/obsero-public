# CLAUDE.md

Claude Code reads this file; every other coding agent reads `AGENTS.md`. To keep
one source of truth, this file only imports it:

@AGENTS.md

Working on one cloud? Also read that cloud's playbook, `aws/AGENTS.md` or
`gcp/AGENTS.md`, before running anything there.
